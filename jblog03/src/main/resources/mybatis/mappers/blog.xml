<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blog">

	<insert id="createBlog" parameterType="uservo">
		<![CDATA[
			insert into blog
			values (#{id }, "${id } 님의 블로그 입니다.", "/assets/upload-images/20231975433395.jpg")
		]]>
	</insert>
	
	<select id="getBlog" parameterType="string" resultType="blogvo">
		<![CDATA[
			select id, title, profile
			from blog
			where id = #{id }
		]]>
	</select>
	
	<update id="updateBlog" parameterType="blogvo">
		<![CDATA[
			update blog
			set title = #{title }, profile = #{profile }
			where id = #{id }
		]]>
	</update>
</mapper>